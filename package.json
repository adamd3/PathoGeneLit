{
  "name": "pathogenelit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "fastapi-dev": "cd api && pip3 install -r requirements.txt && python3 -m uvicorn app:app --reload --port 5328",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "postgraphile": "source .env && postgraphile -c postgres://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME} --watch --enhance-graphiql"
  },
  "dependencies": {
    "@apollo/client": "^3.11.8",
    "graphql": "^16.9.0",
    "next": "14.2.11",
    "postgraphile": "^4.13.0",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.11",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
